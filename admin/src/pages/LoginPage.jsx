import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { useAuth } from "../context/AuthContext";

export function LoginPage() { const { login } = useAuth(); const navigate = useNavigate(); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); const [show, setShow] = useState(false); async function submit(event) { event.preventDefault(); setError(""); setBusy(true); const form = new FormData(event.currentTarget); try { await login(form.get("email"), form.get("password")); navigate("/", { replace: true }); } catch (err) { setError(err.message); } finally { setBusy(false); } } return <div className="login-page"><div className="login-brand"><div className="brand-mark">វ</div><h1>វត្ត អូរត្រាវ</h1><p>ADMIN CMS · ប្រព័ន្ធគ្រប់គ្រងមាតិកា</p></div><form className="login-card" onSubmit={submit}><span className="eyebrow">សូមស្វាគមន៍</span><h2>ចូលប្រព័ន្ធ</h2><p>សូមបញ្ចូលគណនីរបស់អ្នកដើម្បីបន្ត</p>{error && <div className="login-error" role="alert">{error}</div>}<label>អ៊ីមែល<input name="email" required type="email" placeholder="admin@example.com" /></label><label>ពាក្យសម្ងាត់<div className="password-field"><input name="password" required type={show ? "text" : "password"} placeholder="••••••••" /><button type="button" onClick={() => setShow(!show)}>{show ? "លាក់" : "បង្ហាញ"}</button></div></label><div className="login-options"><label><input type="checkbox" /> ចងចាំខ្ញុំ</label><button type="button">ភ្លេចពាក្យសម្ងាត់?</button></div><button className="login-submit" disabled={busy}>{busy ? "កំពុងចូល..." : "ចូលប្រព័ន្ធ →"}</button></form></div>; }
