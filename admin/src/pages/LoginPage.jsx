import { useNavigate } from "react-router-dom";
import { useAuth } from "../context/AuthContext";
export function LoginPage() { const { login } = useAuth(); const navigate = useNavigate(); function submit(event) { event.preventDefault(); login({ id: "ui-session", role: "ADMIN" }); navigate("/", { replace: true }); } return <form onSubmit={submit} style={{ width: 360, padding: 32, background: "white", borderRadius: 16, boxShadow: "0 10px 30px #0001" }}><h1>Admin login</h1><p>Authentication is currently a UI foundation.</p><input required placeholder="Email" type="email" style={{ width: "100%", padding: 12, marginBottom: 12 }} /><input required placeholder="Password" type="password" style={{ width: "100%", padding: 12, marginBottom: 16 }} /><button type="submit" style={{ width: "100%", padding: 12, background: "#11178f", color: "white", border: 0, borderRadius: 8 }}>Sign in</button></form>; }
