import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { useAuth } from "../context/AuthContext";
export function LoginPage() { const { login } = useAuth(); const navigate = useNavigate(); const [error, setError] = useState(""); async function submit(event) { event.preventDefault(); setError(""); const form = new FormData(event.currentTarget); try { await login(form.get("email"), form.get("password")); navigate("/", { replace: true }); } catch (err) { setError(err.message); } } return <form onSubmit={submit} style={{ width: 360, padding: 32, background: "white", borderRadius: 16, boxShadow: "0 10px 30px #0001" }}><h1>Admin login</h1>{error && <p role="alert" style={{ color: "#b42318" }}>{error}</p>}<input name="email" required placeholder="Email" type="email" style={{ width: "100%", padding: 12, marginBottom: 12 }} /><input name="password" required placeholder="Password" type="password" style={{ width: "100%", padding: 12, marginBottom: 16 }} /><button type="submit" style={{ width: "100%", padding: 12, background: "#11178f", color: "white", border: 0, borderRadius: 8 }}>Sign in</button></form>; }
